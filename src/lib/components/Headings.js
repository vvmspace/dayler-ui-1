import React, { Component } from 'react';
import styled from 'styled-components';

export const H1 = styled.span(props => ({
    display: 'block',
    fontFamily: 'Helvetica, Open Sans, sans-serif',
    fontSize: 48,
    fontWeight: 300,
}));

export const H2 = styled.span(props => ({
    display: 'block',
    fontFamily: 'Helvetica, Open Sans, sans-serif',
    fontSize: 40,
    fontWeight: 300,
}));

export const H3 = styled.span(props => ({
    display: 'block',
    fontFamily: 'Helvetica, Open Sans, sans-serif',
    fontSize: 32,
    fontWeight: 300,
}));

export const H4 = styled.span(props => ({
    display: 'block',
    fontFamily: 'Helvetica, Open Sans, sans-serif',
    fontSize: 28,
    fontWeight: 300,
}));

export const H5 = styled.span(props => ({
    display: 'block',
    fontFamily: 'Helvetica, Open Sans, sans-serif',
    fontSize: 18,
    fontWeight: 300,
}));

export const H6 = styled.span(props => ({
    display: 'block',
    fontFamily: 'Helvetica, Open Sans, sans-serif',
    fontSize: 18,
    fontWeight: 300,
}));
